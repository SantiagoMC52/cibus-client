import { Typography } from "@mui/material";
import { MainLayout } from "../layouts";

const Home = () => {
  return (
    <MainLayout>
      <Typography textAlign="center" variant="h3">
        Únete a Cibus, ¿A qué estás esperando?
      </Typography>
    </MainLayout>
  );
};

export default Home;
